<h1>Listing nodes</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Deactivated</th>
    <th>Catalog timestamp</th>
    <th>Facts timestamp</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @nodes.each do |node| %>
  <tr>
    <td><%= node.name %></td>
    <td><%= node.deactivated %></td>
    <td><%= node.catalog_timestamp %></td>
    <td><%= node.facts_timestamp %></td>
    <td><%= link_to 'Show', node  %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Node', new_node_path %>
